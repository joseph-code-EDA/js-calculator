function start () {
	document.getElementById("display").innerHTML='0';//setup HTML
	//document.addEventListener("click", buttonPress);//setup buttonpress function
}

function buttonPress (input) {
	console.log(input);
}